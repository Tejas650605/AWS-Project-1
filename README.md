# AWS-Project-1
create an IAM policy that’s grants an IAM user full amazon EC2 access only when requests target the paris region (eu-west-3),and explicity denies EC2 access in all other regions; then provide step- by step instruction to create and attach that policy using the aws management console.
